import { ImgBase64Pipe } from './img-base64.pipe';

describe('ImgBase64Pipe', () => {
  it('create an instance', () => {
    const pipe = new ImgBase64Pipe();
    expect(pipe).toBeTruthy();
  });
});
